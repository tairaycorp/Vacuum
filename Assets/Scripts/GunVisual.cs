using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GunVisual : MonoBehaviour
{
    bool recoiling = true;
    Transform startPos;
    //public dampTime;
    // Start is called before the first frame update
    void Start()
    {
        //startPos = gameObject.transform;
    }

    // Update is called once per frame
    void Update()
    {
        //transform.position = Vector3.SmoothDamp(transform.position, startPos, Vector3.zero, dampTime);
    }

    void Recoil() {

    }

    void RecoilEvent() {

    }
}
